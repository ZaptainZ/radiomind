# Release-0.2.1 — first public release

**Date:** 2026-06-16
**Author:** Claude Code (cc)
**Scope:** Cut a publishable v0.2.1 from the release-ready state. Docs/version/
build/smoke/tests/tag only — no new features, no retrieval-runtime change, no
RadioHeader repo changes, no managed retrieval, no n=100 benchmark.

## Version decision

- `pyproject.toml` was `0.1.0` (stale — the project self-positioned as "v0.2" and
  the only tag was `v0.2.0-rc1`, never finalized to v0.2.0). `src/radiomind/__init__.py`
  also `0.1.0`.
- No `v0.2.1` tag exists → no conflict. Target **0.2.1** (the task's target, and
  the first proper finalized 0.2.x after the rc). Bumped both version sites
  0.1.0 → 0.2.1.

## Changes

- **`pyproject.toml`** + **`src/radiomind/__init__.py`**: version → `0.2.1`.
- **README.md / README_zh.md**: badge `Pre-release v0.2.0-rc1` → `Release v0.2.1`;
  the version callout `v0.2.0-rc1 (pre-release …)` → `v0.2.1 (2026-06-16)`; release-
  notes links → `/releases/tag/v0.2.1`. Benchmark band, retrieval tiers, remote
  consent, LoRA opt-in, MCP 17, RadioHeader routing were already correct/honest
  (no rewrite). Bilingual parity preserved.
- **RELEASE.md**: added a `v0.2.1 (2026-06-16) — product-entry release` section
  (highlights + honest benchmark restatement + explicit non-goals/parked). Existing
  honest TL;DR/benchmark snapshot kept intact below.

Doc audit (pre-edit) found the benchmark positioning, retrieval口径, MCP count (17),
and managed-retrieval-PARK wording were ALL already correct — no stale `0.930`-as-
standing, no `9 tools`, no "managed live". The earlier ReleaseReadiness-2a and
honest-positioning passes had already cleaned these.

## Build & smoke (clean install of the BUILT artifact)

- `python -m build` (setuptools backend) → `radiomind-0.2.1-py3-none-any.whl` +
  `radiomind-0.2.1.tar.gz`. `dist/` is gitignored (not committed).
- Fresh py3.12 venv, `pip install radiomind-0.2.1-py3-none-any.whl` → version 0.2.1.
- Smoke (all PASS): `radiomind --help`, `python -m radiomind --help`, `onboard`,
  `init`, `learn`, `search running` (finds it), `status` (shows v0.2.1), `doctor`
  (shows v0.2.1).

## Tests

- `regression_pack.py` → ALL PASS.
- Focused product/CLI/retrieval suites → 75 passed.
- **Full `pytest` → 1120 passed, 0 failures.** No optional-dependency noise.

## Tag

- Commit `Release 0.2.1`; annotated tag `v0.2.1`; pushed commit + tag.

## Conclusion

**Released.** v0.2.1 is honest, installable, and entry-clear: clean-install build
smoke green on all entries, full suite green, docs match code, data-egress/quality
boundaries explicit, no misleading claims. Non-goals (managed retrieval/subscription,
hosted vector DB, live RadioHeader digest, v4-pro/hybrid, benchmark chasing) stated
as parked.

See [[project_release_candidate_1a]], [[project_retrieval_ux]],
[[project_managed_retrieval]], [[project_lme_e2e_benchmark]] (benchmark posture).
